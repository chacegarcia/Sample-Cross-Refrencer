/**
 * Entry point for the job scheduler panel (GitHub Pages–friendly: imports resolve from this file's URL).
 */
import { initJobSchedulerPanel } from "./job-scheduler.js";

initJobSchedulerPanel();
